
## Backend

trizen-photo-backend/
├── src/
│   ├── config/
│   │   ├── env.ts              # loads + validates env vars via Zod
│   │   └── constants.ts        # PIN length, max file size, allowed content types
│   │
│   ├── db/
│   │   ├── client.ts           # Drizzle DB client (pg pool + drizzle())
│   │   ├── schema.ts           # all tables in Drizzle DSL
│   │   └── migrations/         # generated by drizzle-kit
│   │
│   ├── redis/
│   │   └── client.ts           # Upstash Redis client
│   │
│   ├── s3/
│   │   └── client.ts           # AWS S3 client, presigned URL helpers
│   │
│   ├── middleware/
│   │   ├── auth.ts             # session cookie → req.user
│   │   ├── rbac.ts             # requireAdmin, requireTeamMember, etc.
│   │   ├── rateLimit.ts        # login + PIN verify rate limiters
│   │   ├── errorHandler.ts     # centralized error → JSON response
│   │   └── validate.ts         # Zod schema validator middleware
│   │
│   ├── modules/
│   │   ├── auth/
│   │   │   ├── auth.routes.ts
│   │   │   ├── auth.controller.ts
│   │   │   ├── auth.service.ts
│   │   │   ├── auth.schemas.ts       # Zod request schemas
│   │   │   └── session.ts            # session create/read/delete (Redis)
│   │   │
│   │   ├── users/                    # team-member management
│   │   │   ├── users.routes.ts
│   │   │   ├── users.controller.ts
│   │   │   ├── users.service.ts
│   │   │   └── users.schemas.ts
│   │   │
│   │   ├── events/
│   │   │   ├── events.routes.ts
│   │   │   ├── events.controller.ts
│   │   │   ├── events.service.ts
│   │   │   ├── events.schemas.ts
│   │   │   └── event-members.service.ts
│   │   │
│   │   ├── photos/
│   │   │   ├── photos.routes.ts
│   │   │   ├── photos.controller.ts
│   │   │   ├── photos.service.ts
│   │   │   └── photos.schemas.ts
│   │   │
│   │   ├── galleries/
│   │   │   ├── galleries.routes.ts
│   │   │   ├── galleries.controller.ts
│   │   │   ├── galleries.service.ts
│   │   │   ├── galleries.schemas.ts
│   │   │   └── pin.ts                # crypto.randomInt + bcrypt helpers
│   │   │
│   │   └── public-gallery/           # customer-facing endpoints
│   │       ├── public.routes.ts
│   │       ├── public.controller.ts
│   │       ├── public.service.ts
│   │       ├── public.schemas.ts
│   │       └── gallery-session.ts    # separate Redis session for customer
│   │
│   ├── utils/
│   │   ├── crypto.ts           # bcrypt wrapper, token generation
│   │   ├── errors.ts           # AppError class + error codes
│   │   └── logger.ts           # pino logger
│   │
│   ├── types/
│   │   └── express.d.ts        # augment Express Request with user, session
│   │
│   ├── jobs/
│   │   └── cleanupPendingPhotos.ts   # runs periodically
│   │
│   ├── app.ts                  # express() setup, middleware chain, mount routes
│   └── index.ts                # server.listen()
│
├── tests/
│   ├── auth.test.ts
│   ├── authorization.test.ts
│   ├── photos.test.ts
│   ├── galleries.test.ts
│   └── setup.ts
│
├── drizzle.config.ts
├── package.json
├── tsconfig.json
├── .env.example                # committed; real .env is gitignored
├── .gitignore
└── README.md


## Frontend

trizen-photo-frontend/
├── app/
│   ├── (auth)/
│   │   ├── login/page.tsx
│   │   └── register/page.tsx
│   │
│   ├── (dashboard)/                  # authenticated area — admin + team member
│   │   ├── layout.tsx                # sidebar, top bar, session guard
│   │   ├── events/
│   │   │   ├── page.tsx              # list events (role-aware)
│   │   │   ├── new/page.tsx          # admin only
│   │   │   └── [eventId]/
│   │   │       ├── page.tsx          # event details
│   │   │       ├── photos/page.tsx   # upload (team) / review (admin)
│   │   │       ├── members/page.tsx  # admin only
│   │   │       └── gallery/page.tsx  # admin only
│   │
│   ├── gallery/
│   │   └── [publicToken]/
│   │       ├── page.tsx              # PIN entry
│   │       └── view/page.tsx         # gallery viewer (post-PIN)
│   │
│   ├── layout.tsx                    # root
│   ├── page.tsx                      # landing
│   └── globals.css
│
├── components/
│   ├── ui/                           # reusable primitives (button, input, dialog)
│   ├── PhotoGrid.tsx
│   ├── PhotoUploader.tsx
│   ├── GalleryList.tsx
│   └── PinInput.tsx
│
├── lib/
│   ├── api.ts                        # fetch wrapper, error shape parsing
│   ├── auth-context.tsx              # who-am-I context provider
│   ├── s3-upload.ts                  # helper: PUT to presigned URL
│   └── types.ts                      # shared TS types (mirror backend responses)
│
├── public/
├── package.json
├── tsconfig.json
├── next.config.js
├── tailwind.config.js
├── .env.example
├── .gitignore
└── README.md


## Github repo structure

trizen-photo-platform/
├── backend/
├── frontend/
├── docs/
├── README.md
└── .gitignore